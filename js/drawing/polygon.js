export default function drawPoly() {
    console.log("폴리곤 그리기");
    // var line = toString.makeLine(clickPos[0].x, clickPos[0].y, clickPos[1].x, clickPos[1].y);
    // line.linewidth = 1;
    // line.fill = "black";
    // t.update();
    // endPos = clickPos[1];
    // undoSavePOs = clickPos[0];
    // clickPos.length = 0;
    // clickPos.push(endPos);
}